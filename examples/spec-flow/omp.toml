id = "examples.spec-flow"
entry = "spec_flow"
version = "0.1.0"
description = "Journal-backed, artifact-gated specification workflow with blob archival."
requires = []
capabilities = ["env.doc.read", "env.doc.write", "env.blob", "journal.append"]

[[tools]]
name = "flow"
kind = "soft"
family = "spec"
rev = 1
module = "spec_flow"
summary = "Run a brainstorm, plan, apply, and archive specification workflow."
