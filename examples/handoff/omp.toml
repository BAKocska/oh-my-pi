id = "examples.handoff"
entry = "handoff"
version = "0.1.0"
description = "Issue a bounded clean-context implementation handoff to a budgeted child."
requires = []
capabilities = ["agents.spawn", "journal.append", "ui.commands"]

[[declarations]]
id = "handoff"
kind = "command"
module = "handoff"
key = "handoff"
trigger = "lazy"
api = 1
failure = "fault"
