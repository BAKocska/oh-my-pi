id = "examples.permission-rules"
entry = "permission_rules"
kind = "extension"
version = "0.1.0"
description = "Apply declarative allow, deny, and approval rules to analyzed shell calls."
requires = []
capabilities = []

[[hooks]]
event = "tool_call"
phase = "precheck"
failure = "fail-closed"

[[hooks]]
event = "tool_call"
phase = "approval"
failure = "fail-closed"
