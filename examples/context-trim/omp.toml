id = "examples.context-trim"
entry = "context_trim"
version = "0.1.0"
description = "Restore context headroom with bounded projection patches over old tool outputs."
requires = []
capabilities = []

[[hooks]]
event = "thread_projection"
phase = "domain"
order = 0
failure = "fail-open"
