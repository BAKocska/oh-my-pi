id = "examples.todo-journal"
entry = "todo_journal"
kind = "extension"
version = "0.1.0"
description = "A journal-backed todo list with no parallel cache or state file."
requires = []
capabilities = ["journal.append"]

[[tools]]
name = "todo"
kind = "soft"
family = "v"
rev = 1
module = "todo_journal"
summary = "Add, complete, or drop todo items and return the current checklist."
