id = "examples.statusline"
entry = "statusline"
kind = "extension"
version = "0.1.0"
description = "Coalesced statusline and footer segments backed by session usage facts."
requires = []
capabilities = ["ui.slots"]

[[hooks]]
event = "extension_activate"
phase = "observe"
failure = "fail-open"

[[hooks]]
event = "turn_end"
phase = "observe"
failure = "fail-open"
