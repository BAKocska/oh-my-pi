id = "examples.workflows"
entry = "workflows"
version = "0.1.0"
description = "Run resumable, budgeted subagent DAGs in atomic topological waves."
requires = []
capabilities = ["agents.spawn", "journal.append"]

[[tools]]
name = "workflow"
kind = "soft"
family = "agents"
rev = 1
module = "workflows"
summary = "Run or resume a declarative subagent DAG."

[settings.workflow]
type = "string"
default = ""
description = "A JSON object containing name, nodes, and dependency edges."
