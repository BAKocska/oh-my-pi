id = "examples.subagents"
entry = "subagents"
kind = "extension"
version = "0.1.0"
description = "Run budgeted subagent waves through the core-owned agent tree."
requires = []
capabilities = ["agents.spawn"]

[[tools]]
name = "swarm"
kind = "soft"
family = "agents"
rev = 1
module = "subagents"
summary = "Run, inspect, steer, and harvest one budgeted subagent wave."
