id = "examples.auto-thinking"
entry = "auto_thinking"
version = "0.1.0"
description = "Adjusts per-turn reasoning effort with bounded task classification."
requires = []
capabilities = ["inference:completion"]

[[hooks]]
event = "turn_start"
phase = "transform"
module = "auto_thinking"
failure = "fail-open"
