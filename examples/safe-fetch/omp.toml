id = "examples.safe-fetch"
entry = "safe_fetch"
kind = "extension"
version = "0.1.0"
description = "Defensively fetch public web pages with per-hop address validation."
requires = []
capabilities = ["place_env", "env.net"]

[[tools]]
name = "fetch_page"
kind = "soft"
family = "safe"
rev = 1
module = "safe_fetch"
summary = "Fetch a public page after validating every resolved address and redirect hop."
