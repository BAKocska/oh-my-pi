id = "examples.rewind"
entry = "rewind"
version = "0.1.0"
description = "Journal-backed workspace checkpoints and coordinated conversation rewinds."
requires = []
capabilities = ["env.workspace.snapshot", "journal.append"]

[[tools]]
name = "checkpoint"
kind = "soft"

[[tools]]
name = "rewind"
kind = "soft"
