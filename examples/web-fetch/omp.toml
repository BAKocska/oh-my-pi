id = "examples.web-fetch"
entry = "web_fetch"
kind = "extension"
version = "0.1.0"
description = "Fetch public web pages as structured, spill-safe text verdicts."
requires = []
capabilities = ["place_env", "env.net"]

[[tools]]
name = "fetch_web"
kind = "soft"
family = "v"
rev = 1
module = "web_fetch"
summary = "Fetch a public HTTP or HTTPS page and return its title and extracted text."
