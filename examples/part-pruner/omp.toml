id = "examples.part-pruner"
entry = "part_pruner"
version = "0.1.0"
description = "Drop stale model-facing tool-result parts while retaining typed verdicts."
requires = []
capabilities = []

[[hooks]]
event = "thread_projection"
phase = "domain"
order = 0
failure = "fail-open"
