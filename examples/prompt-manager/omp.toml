id = "examples.prompt-manager"
entry = "prompt_manager"
version = "0.1.0"
description = "User-scoped reusable prompts with native command completion and safe composer insertion."
requires = []
capabilities = ["journal.append", "ui.commands"]

[[declarations]]
id = "prompt"
kind = "command"
module = "prompt_manager"
key = "prompt"
trigger = "lazy"
api = 1
failure = "fault"
