id = "examples.trust-gates"
entry = "trust_gates"
version = "0.1.0"
description = "A fail-soft ambient file probe that widens only for trusted children."
requires = []
capabilities = []

[[tools]]
name = "trust_read"
kind = "soft"
family = "trust"
rev = 1
module = "trust_gates"
summary = "Preview an ambient file when the extension child is trusted."
