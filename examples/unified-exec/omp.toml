id = "examples.unified-exec"
entry = "unified_exec"
version = "0.1.0"
description = "Environment-supervised interactive PTY sessions with cursor polling."
requires = []
capabilities = ["env.process", "env.blob"]

[[tools]]
name = "session"
kind = "soft"
family = "unified-exec"
rev = 1
module = "unified_exec"
summary = "Open, poll, drive, signal, or close a named interactive PTY session."

[settings]
