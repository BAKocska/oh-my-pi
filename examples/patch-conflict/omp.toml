id = "examples.patch-conflict"
entry = "patch_conflict"
version = "0.1.0"
description = "Probe conflicting context patches and stale compaction epochs."
requires = []
capabilities = []

[[hooks]]
event = "thread_projection"
phase = "domain"
order = 0
failure = "fail-open"
