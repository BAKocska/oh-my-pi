id = "examples.web-dashboard"
entry = "web_dashboard"
version = "0.1.0"
description = "Environment-supervised session and usage JSON dashboard."
requires = []
capabilities = ["env.process", "sessions.read"]

[[tools]]
name = "dashboard"
kind = "soft"
family = "web"
rev = 1
module = "web_dashboard"
summary = "Refresh and report the supervised session usage dashboard."

[settings]
