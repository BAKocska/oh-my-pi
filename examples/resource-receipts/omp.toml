id = "examples.resource-receipts"
entry = "resource_receipts"
version = "0.1.0"
description = "Live extension resource receipts with hard-quota degradation."
requires = []
capabilities = ["journal.append"]

[[tools]]
name = "resource_receipts"
kind = "soft"
family = "resources"
rev = 1
module = "resource_receipts"
summary = "Inspect this extension's live quota receipt and optionally record a probe."
