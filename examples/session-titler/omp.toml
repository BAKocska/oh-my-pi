id = "examples.session-titler"
entry = "session_titler"
version = "0.1.0"
description = "Generate one concise session title after the first settled turn."
requires = []
capabilities = ["inference:completion", "journal.append", "ui.title"]

[[hooks]]
event = "turn_end"
phase = "observe"
order = 0
failure = "fail-open"
