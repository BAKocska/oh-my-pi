id = "examples.sidebar"
entry = "sidebar"
version = "0.1.0"
description = "Retained right rail for live session-tree and usage facts."
requires = []
capabilities = ["ui.slots"]

[[hooks]]
event = "extension_activate"
phase = "observe"
order = 0
failure = "fail-open"

[[telemetry]]
kinds = ["session_start", "turn_start", "turn_end", "session_end"]
scope = "tree"
queue = 256
overflow = "coalesce_by_key"
replay = true
replay_limit = 512
