id = "examples.patch-dialect"
entry = "patch_dialect"
kind = "extension"
version = "0.1.0"
description = "A third revisioned edit dialect with direct replace and hashline lift paths."
requires = []
capabilities = ["place.env", "env.doc.read", "env.doc.write"]

[[tools]]
name = "edit"
kind = "soft"
family = "patch"
rev = 1
module = "patch_dialect"
summary = "Apply resolved byte-range patches through a revision-pinned document lease."
