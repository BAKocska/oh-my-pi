id = "examples.determinism-probe"
entry = "determinism_probe"
version = "0.1.0"
description = "Swept repeat-and-perturb probes for byte-stable extension contracts."
requires = []
capabilities = []

[[tools]]
name = "determinism_probe"
kind = "soft"
family = "det"
rev = 1
module = "determinism_probe"
summary = "Run repeat/perturb byte-stability checks over pure extension contracts."
