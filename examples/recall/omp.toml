id = "examples.recall"
entry = "recall"
kind = "extension"
version = "0.1.0"
description = "Queries settled outcomes by exact tool revision and typed fields."
requires = []
capabilities = ["telemetry.project_scope", "telemetry.historical"]

[[tools]]
name = "recall"
kind = "soft"
family = "structured"
rev = 1
module = "recall"
summary = "Recall settled outcomes from one exact tool revision."
