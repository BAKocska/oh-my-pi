id = "examples.plan-mode"
entry = "plan_mode"
kind = "extension"
version = "0.1.0"
description = "A session-scoped read-only planning mode with typed plan handoff."
requires = []
capabilities = []

[[declarations]]
id = "plan-mode"
kind = "regime"
module = "plan_mode"
key = "plan-mode"
trigger = "lazy"
api = 1
failure = "fail-closed"

[[declarations]]
id = "plan-settle-gate"
kind = "regime"
module = "plan_mode"
key = "plan-settle-gate"
trigger = "lazy"
api = 1
failure = "fail-open"

[[tools]]
name = "plan"
kind = "soft"
