id = "examples.org-registry"
entry = "org_registry"
version = "0.1.0"
description = "Organization convention registry with user-scoped denial degradation."
requires = []
capabilities = ["journal.append"]

[[tools]]
name = "org_registry"
kind = "soft"
family = "organization"
rev = 1
module = "org_registry"
summary = "Read and journal shared organization convention pins."
