id = "examples.fresh-loop"
entry = "fresh_loop"
kind = "extension"
version = "0.1.0"
description = "Repeat one prompt in clean, budgeted child sessions until a durable stop verdict."
requires = []
capabilities = ["agents.spawn", "inference:completion", "journal.append"]

[[tools]]
name = "fresh_loop"
kind = "soft"
