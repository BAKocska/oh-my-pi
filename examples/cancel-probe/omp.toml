id = "examples.cancel-probe"
entry = "cancel_probe"
version = "0.1.0"
description = "Cancellation conformance probe across the seven invocation phases."
requires = []
capabilities = ["place.env", "env.exec"]

[[tools]]
name = "cancel_probe"
kind = "soft"
family = "cancel"
rev = 1
module = "cancel_probe"
summary = "Hold an authorized device body or guarded Run open for external cancellation."
