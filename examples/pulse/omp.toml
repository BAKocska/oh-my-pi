id = "examples.pulse"
entry = "pulse"
version = "0.1.0"
description = "Post-hoc model TPS, TTFT, and response-time footer EMAs."
requires = []
capabilities = ["ui.slots"]

[[telemetry]]
kinds = ["model_request"]
scope = "tree"
queue = 128
overflow = "coalesce_by_key"
