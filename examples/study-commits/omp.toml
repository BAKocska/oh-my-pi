id = "examples.study-commits"
entry = "study_commits"
version = "0.1.0"
description = "Recent-commit multi-select with bounded diff injection and lossless spill handling."
requires = []
capabilities = ["env.exec", "env.blob", "ui.commands", "ui.dialogs"]

[[declarations]]
id = "commits"
kind = "command"
module = "study_commits"
key = "commits"
trigger = "lazy"
api = 1
failure = "fault"
