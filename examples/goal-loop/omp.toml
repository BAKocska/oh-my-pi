id = "examples.goal-loop"
entry = "goal_loop"
kind = "extension"
version = "0.1.0"
description = "A session-scoped autonomous goal regime with durable settle control."
requires = []
capabilities = []

[[tools]]
name = "goal"
kind = "soft"

[[declarations]]
id = "goal-loop"
kind = "regime"
module = "goal_loop"
key = "goal-loop"
trigger = "lazy"
api = 1
failure = "fail-open"
