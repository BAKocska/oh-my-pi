id = "examples.edit-dialect"
entry = "edit_dialect"
kind = "extension"
version = "0.1.0"
description = "An alternate revisioned hashline edit dialect backed by document leases."
requires = []
capabilities = ["place.env", "env.doc.read", "env.doc.write"]

[[tools]]
name = "edit"
kind = "soft"
family = "hlx"
rev = 1
module = "edit_dialect"
summary = "Apply an HLX patch through one revision-pinned document lease."
