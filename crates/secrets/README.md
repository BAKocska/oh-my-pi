# omp-secrets

`omp-secrets` owns the closed secret-rule contract and allocation-conscious primitives used to mask provider-bound text and restore keyed placeholders. Persistence and process policy stay in `omp-storage` and `omp-app`; this crate is deterministic and side-effect free.
