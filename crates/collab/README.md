# omp-collab

`omp-collab` owns OMP's versioned, bounded collaboration relay substrate: room cryptography, protobuf framing, deterministic replication reduction, and reconnect transport state. It deliberately has no application or UI dependencies and implements only the native `omp.collab.v1` wire.
